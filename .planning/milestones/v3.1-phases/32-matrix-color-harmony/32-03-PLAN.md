---
phase: 32-matrix-color-harmony
plan: 03
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - apps/web/components/portfolio/footer.tsx
  - apps/web/components/portfolio/tech-stack.tsx
  - apps/web/components/portfolio/parallax-divider.tsx
  - apps/web/app/(portfolio)/page.tsx
autonomous: true
requirements:
  - COLOR-05
  - COLOR-06
  - COLOR-07

must_haves:
  truths:
    - "Footer nav and social icon hover states use Matrix green — no hover:text-primary remains in footer.tsx"
    - "Tech-stack card hover border uses Matrix green border token"
    - "ParallaxDivider line uses Matrix green border token instead of bg-primary/30"
    - "Homepage featured project card borders use Matrix green border token"
    - "Homepage featured project card gradient backgrounds use Matrix green subtle token"
    - "Homepage case study read links use Matrix green instead of blue"
    - "Homepage stat numbers are font-mono Matrix green terminal token"
    - "Homepage Featured Projects h2 has SectionLabel prefix"
  artifacts:
    - path: "apps/web/components/portfolio/footer.tsx"
      provides: "Footer with Matrix green hover states — COLOR-06 complete"
    - path: "apps/web/components/portfolio/tech-stack.tsx"
      provides: "Tech-stack badges with Matrix green hover border — COLOR-05 partial"
    - path: "apps/web/components/portfolio/parallax-divider.tsx"
      provides: "Parallax divider line using Matrix green border token"
    - path: "apps/web/app/(portfolio)/page.tsx"
      provides: "Homepage with Matrix green — featured cards, stats, SectionLabel on Featured Projects h2"
  key_links:
    - from: "apps/web/app/(portfolio)/page.tsx"
      to: "apps/web/components/portfolio/section-label.tsx"
      via: "import { SectionLabel } from '@/components/portfolio/section-label'"
      pattern: "SectionLabel"
    - from: "apps/web/components/portfolio/parallax-divider.tsx"
      to: ".matrix-theme CSS token"
      via: "bg-[var(--matrix-green-border)] Tailwind arbitrary"
      pattern: "matrix-green-border"
---

<objective>
Apply Matrix color harmony to footer hover states (COLOR-06), tech-stack and parallax-divider components (COLOR-05), the homepage (COLOR-05 + COLOR-07), completing all blue-to-green token swaps in shared portfolio components.

Purpose: Parallel to 32-02 — no shared files. This plan finishes the component-level and homepage sweep. Footer 9 hover-text-primary → Matrix green, parallax-divider bg-primary/30 fix (this was always meant to be Matrix green, now it's explicit), homepage cards/stats/SectionLabel.

Output: footer.tsx, tech-stack.tsx, parallax-divider.tsx, and homepage page.tsx fully Matrix green with no blue primary references.
</objective>

<execution_context>
@/home/doctor/.claude/get-shit-done/workflows/execute-plan.md
@/home/doctor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/32-matrix-color-harmony/32-RESEARCH.md
@.planning/phases/32-matrix-color-harmony/32-01-SUMMARY.md

@apps/web/components/portfolio/footer.tsx
@apps/web/components/portfolio/tech-stack.tsx
@apps/web/components/portfolio/parallax-divider.tsx
@apps/web/app/(portfolio)/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Footer (COLOR-06) + tech-stack + parallax-divider (COLOR-05)</name>
  <files>
    apps/web/components/portfolio/footer.tsx
    apps/web/components/portfolio/tech-stack.tsx
    apps/web/components/portfolio/parallax-divider.tsx
  </files>
  <action>
**footer.tsx — COLOR-06 (9 instances):**

Read the file. Research confirmed these patterns to replace:

1. **Nav links** (research: lines 30, 38, 46, 54 — `hover:text-primary`):
   Find all occurrences of `hover:text-primary` on nav Link elements. Replace each with `hover:text-[var(--matrix-green)]`.
   Pattern to find: `className="text-sm text-muted-foreground hover:text-primary transition-colors"`
   Replace with: `className="text-sm text-muted-foreground hover:text-[var(--matrix-green)] transition-colors"`

2. **Social icon links** (research: lines 72, 81 — `hover:text-primary`):
   Same replacement: `hover:text-primary` → `hover:text-[var(--matrix-green)]`.

After replacement, verify: `grep -n "hover:text-primary" apps/web/components/portfolio/footer.tsx` returns 0 results.

**tech-stack.tsx — COLOR-05:**

Read the file. Research confirmed line 29: `hover:border-primary/50` on the Card component.
Replace: `hover:border-primary/50` → `hover:border-[var(--matrix-green-border)]`

**parallax-divider.tsx — COLOR-05:**

Read the file. Research confirmed line 44: `bg-primary/30` on the divider line element.
Replace: `bg-primary/30` → `bg-[var(--matrix-green-border)]`

Context on this change: Phase 30 STATE.md comment said "Matrix green at 30% opacity within .matrix-theme" but `bg-primary/30` in `.matrix-theme` context resolves to Tailwind's `--primary` (blue-11 via @theme inline). The token `--matrix-green-border` is `#00FF4133` (~20% opacity Matrix green) — close enough to 30% and now semantically correct. This aligns the actual rendered color with the original intent.
  </action>
  <verify>
grep -n "hover:text-primary\|hover:border-primary\|bg-primary" apps/web/components/portfolio/footer.tsx
grep -n "hover:border-primary\|primary" apps/web/components/portfolio/tech-stack.tsx
grep -n "bg-primary\|primary" apps/web/components/portfolio/parallax-divider.tsx
cd apps/web && npx tsc --noEmit 2>&1 | head -20
  </verify>
  <done>footer.tsx has zero hover:text-primary instances. tech-stack.tsx hover border uses Matrix green border token. parallax-divider.tsx uses bg-[var(--matrix-green-border)]. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Homepage (COLOR-05 + COLOR-07) — featured cards, stats, SectionLabel</name>
  <files>
    apps/web/app/(portfolio)/page.tsx
  </files>
  <action>
Read the file first. Research confirmed these locations to fix in `apps/web/app/(portfolio)/page.tsx`:

1. **Add import** at top: `import { SectionLabel } from '@/components/portfolio/section-label';`

2. **Featured project card borders** (research: lines 41, 90 — `border-2 border-primary`):
   Replace `border-primary` with `border-[var(--matrix-green-border)]` on both featured project card wrapper divs.

3. **Featured project card gradients** (research: lines 41, 90 — `from-primary/5 to-transparent`):
   Replace `from-primary/5` with `from-[var(--matrix-green-subtle)] to-transparent` on both.

4. **Case study read links** (research: lines 79, 127 — `text-primary` on "Read full case study →"):
   Replace `text-primary` with `text-[var(--matrix-green)]`.

5. **Homepage stat numbers** (research: lines 145, 153, 161 — `text-4xl font-bold text-primary`):
   Replace `text-primary` with `font-mono text-[var(--matrix-terminal)]` on each stat div. Result: `text-4xl font-bold font-mono text-[var(--matrix-terminal)]`.

6. **SectionLabel above Featured Projects h2** (research: line 26 — `<h2 className="text-3xl md:text-4xl font-bold text-foreground mb-4">`):
   The h2 text is "Featured Projects". Add `<SectionLabel label="Featured Projects" />` directly above the h2 element.

After all changes: `grep -n "text-primary\|border-primary\|from-primary\|bg-primary" apps/web/app/(portfolio)/page.tsx` must return 0 results.
  </action>
  <verify>
grep -n "text-primary\|border-primary\|from-primary\|bg-primary" apps/web/app/(portfolio)/page.tsx
grep -n "SectionLabel" apps/web/app/(portfolio)/page.tsx
grep -n "matrix-terminal\|font-mono" apps/web/app/(portfolio)/page.tsx | grep "stat\|font-bold" | head -5
cd apps/web && npx tsc --noEmit 2>&1 | head -20
  </verify>
  <done>Homepage has zero primary color references. SectionLabel appears above the Featured Projects h2. Stat numbers use font-mono Matrix green. TypeScript clean.</done>
</task>

</tasks>

<verification>
1. `grep -rn "hover:text-primary" apps/web/components/portfolio/footer.tsx` — 0 results
2. `grep -rn "primary" apps/web/components/portfolio/parallax-divider.tsx` — 0 results
3. `grep -rn "text-primary\|border-primary\|from-primary\|bg-primary" apps/web/app/(portfolio)/page.tsx` — 0 results
4. `grep -n "SectionLabel" apps/web/app/(portfolio)/page.tsx` — at least 2 lines (import + usage)
5. `cd apps/web && npx tsc --noEmit` — clean
</verification>

<success_criteria>
- Footer: all 9 hover:text-primary replaced with hover:text-[var(--matrix-green)]
- Tech-stack: hover border uses Matrix green border token
- ParallaxDivider: bg-primary/30 replaced with bg-[var(--matrix-green-border)]
- Homepage: featured card borders + gradients + stat numbers + read links all Matrix green
- Homepage: SectionLabel prefix above Featured Projects h2
- TypeScript compilation clean across all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/32-matrix-color-harmony/32-03-SUMMARY.md` following the summary template.
</output>
