---
phase: 10-component-migration-portfolio
plan: "03"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - apps/web/app/(portfolio)/page.tsx
  - apps/web/app/(portfolio)/about/page.tsx
  - apps/web/app/(portfolio)/resume/page.tsx
  - apps/web/app/(portfolio)/projects/page.tsx
  - apps/web/app/(portfolio)/projects/teamflow/page.tsx
autonomous: true
requirements:
  - MIG-01

must_haves:
  truths:
    - "Portfolio homepage uses Badge for tech tags and featured markers, not inline <span> elements"
    - "About page uses Card for value-proposition boxes and Button asChild for the CTA"
    - "Resume page uses Button asChild for the download link, not raw <a> with bg-blue-600"
    - "Projects page renders migrated ProjectCard component (no remaining old import path)"
    - "TeamFlow case study page uses Button asChild for CTAs, no bg-blue-* hardcoded classes"
    - "grep for old hardcoded color patterns in app/(portfolio) returns zero results"
  artifacts:
    - path: "apps/web/app/(portfolio)/page.tsx"
      provides: "Homepage using Badge for tech/status indicators"
      contains: "from '@/components/ui/badge'"
    - path: "apps/web/app/(portfolio)/about/page.tsx"
      provides: "About page using Card + Button asChild"
      contains: "from '@/components/ui/card'"
    - path: "apps/web/app/(portfolio)/resume/page.tsx"
      provides: "Resume page with Button asChild download link"
      contains: "from '@/components/ui/button'"
    - path: "apps/web/app/(portfolio)/projects/page.tsx"
      provides: "Projects page using migrated ProjectCard"
      contains: "ProjectCard"
    - path: "apps/web/app/(portfolio)/projects/teamflow/page.tsx"
      provides: "TeamFlow page with Button asChild CTAs and semantic tokens"
      contains: "from '@/components/ui/button'"
  key_links:
    - from: "apps/web/app/(portfolio)/page.tsx"
      to: "apps/web/components/ui/badge.tsx"
      via: "Badge import for tech tags and featured markers"
      pattern: "from '@/components/ui/badge'"
    - from: "apps/web/app/(portfolio)/about/page.tsx"
      to: "apps/web/components/ui/card.tsx"
      via: "Card sub-components for value-proposition boxes"
      pattern: "from '@/components/ui/card'"
    - from: "apps/web/app/(portfolio)/resume/page.tsx"
      to: "apps/web/components/ui/button.tsx"
      via: "Button asChild wrapping <a> for download"
      pattern: "Button asChild"
    - from: "apps/web/app/(portfolio)/projects/teamflow/page.tsx"
      to: "apps/web/components/ui/button.tsx"
      via: "Button asChild wrapping <a> for GitHub and live demo links"
      pattern: "Button asChild"
---

<objective>
Migrate all five portfolio page files from hardcoded Tailwind color classes and raw HTML elements to Shadcn UI primitives using semantic design tokens.

Purpose: Pages depend on the migrated components from Plan 01 (now complete as a prerequisite). This is the final migration step that achieves the MIG-01 requirement: zero old-pattern imports in app/(portfolio).
Output: Five page files migrated. Zero hardcoded color classes in the entire portfolio scope (components/ + app/(portfolio)).
</objective>

<execution_context>
@/home/doctor/.claude/get-shit-done/workflows/execute-plan.md
@/home/doctor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-component-migration-portfolio/10-RESEARCH.md
@apps/web/DESIGN-SYSTEM.md
@.planning/phases/10-component-migration-portfolio/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate homepage, about, and resume pages</name>
  <files>
    apps/web/app/(portfolio)/page.tsx
    apps/web/app/(portfolio)/about/page.tsx
    apps/web/app/(portfolio)/resume/page.tsx
  </files>
  <action>
Read each file before modifying. Apply changes inline — do not create new components.

**page.tsx (homepage):**
From the research audit, this file has:
- Inline `<span>` for "Featured" badge → replace with `<Badge variant="default">`
- Inline `<span>` for tech stack badges → replace with `<Badge variant="secondary">`
- `text-gray-600 dark:text-gray-300` → `text-muted-foreground`

Add imports at top:
```tsx
import { Badge } from '@/components/ui/badge'
```

Replace each `<span className="... rounded-full bg-primary ...">Featured</span>` → `<Badge variant="default">Featured</Badge>`
Replace each tech tag span like `<span className="... bg-gray-100 ...">Next.js</span>` → `<Badge variant="secondary">Next.js</Badge>`
Replace all `text-gray-600 dark:text-gray-300` → `text-muted-foreground`

**about/page.tsx:**
From the research audit, this file has:
- Inline `<div>` cards (border rounded-lg) for values section → replace with Card, CardHeader, CardTitle, CardContent from '@/components/ui/card'
- `<a>` for CTA button → replace with `<Button asChild>` from '@/components/ui/button'
- `text-gray-600 dark:text-gray-300` → `text-muted-foreground`

Pattern for value/feature cards:
```tsx
import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card'

// Before: <div className="border rounded-lg p-6 ...">
// After:
<Card>
  <CardHeader>
    <CardTitle>{title}</CardTitle>
  </CardHeader>
  <CardContent>
    <p className="text-muted-foreground">{description}</p>
  </CardContent>
</Card>
```

Pattern for CTA link:
```tsx
import { Button } from '@/components/ui/button'
import Link from 'next/link'

// Before: <a href="/contact" className="inline-flex ... bg-primary ...">
// After:
<Button asChild size="lg">
  <Link href="/contact">Get In Touch</Link>
</Button>
```

**resume/page.tsx:**
From the research audit, this file has:
- `bg-blue-600` classes → `bg-primary`
- `text-gray-*` → `text-muted-foreground` or `text-foreground`
- `bg-gray-50` → `bg-muted`
- Raw `<a>` download button with `bg-blue-600 text-white` → `<Button asChild>` from '@/components/ui/button'

Pattern for download button:
```tsx
import { Button } from '@/components/ui/button'

// Before: <a href="/resume.pdf" className="... bg-blue-600 text-white ...">Download PDF</a>
// After:
<Button asChild>
  <a href="/resume.pdf" download>Download PDF</a>
</Button>
```

Apply token mapping for all remaining hardcoded classes per DESIGN-SYSTEM.md.

After all 3 files, run the verification grep on just these files.
  </action>
  <verify>
```bash
# Zero hardcoded color patterns in migrated pages
grep -n "bg-white\|bg-gray-\|text-gray-\|border-gray-\|bg-blue-\|dark:bg-gray\|dark:text-gray" \
  /home/doctor/fernandomillan/apps/web/app/\(portfolio\)/page.tsx \
  /home/doctor/fernandomillan/apps/web/app/\(portfolio\)/about/page.tsx \
  /home/doctor/fernandomillan/apps/web/app/\(portfolio\)/resume/page.tsx

# TypeScript compiles
cd /home/doctor/fernandomillan && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20
```
  </verify>
  <done>
grep for hardcoded color patterns returns zero results for homepage, about, and resume page files. TypeScript compilation passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate projects page and TeamFlow case study page</name>
  <files>
    apps/web/app/(portfolio)/projects/page.tsx
    apps/web/app/(portfolio)/projects/teamflow/page.tsx
  </files>
  <action>
Read each file before modifying.

**projects/page.tsx:**
The research audit notes this page imports the custom `ProjectCard` from `@/components/portfolio/project-card`. Since Plan 01 migrated `project-card.tsx` internally to use Shadcn primitives, the import in this page file already points to the migrated component. Verify this is still the case — if so, only semantic token replacement on any inline JSX in this file is needed.

Check if the page itself has any hardcoded color classes in its own JSX (not inside ProjectCard). If yes, apply token mapping. If the page is clean (just renders ProjectCard with props), only a verification grep is needed.

Do NOT change the `ProjectCard` import path — it should remain `@/components/portfolio/project-card`. The migration was done inside the component file.

**projects/teamflow/page.tsx:**
From the research audit, this file has:
- `bg-blue-600` → `bg-primary`
- `bg-blue-50 dark:bg-blue-950/30` → `bg-accent`
- `border-gray-*` → `border-border`
- `text-gray-*` → `text-muted-foreground`
- Raw `<a>` CTAs for GitHub and live demo links → `<Button asChild variant="outline">` for secondary CTAs

Apply token replacements and replace raw `<a>` CTA buttons with:
```tsx
import { Button } from '@/components/ui/button'

// GitHub link
<Button asChild variant="outline">
  <a href="https://github.com/..." target="_blank" rel="noopener noreferrer">
    View GitHub
  </a>
</Button>

// Live demo link
<Button asChild>
  <a href="https://..." target="_blank" rel="noopener noreferrer">
    Live Demo
  </a>
</Button>
```

After both files, run the FINAL comprehensive verification grep across ALL portfolio pages and components to confirm zero remaining hardcoded patterns.
  </action>
  <verify>
```bash
# Final comprehensive zero-pattern check across ALL portfolio scope
grep -rn "bg-white\|bg-gray-\|text-gray-\|border-gray-\|bg-blue-\|dark:bg-gray\|dark:text-gray" \
  /home/doctor/fernandomillan/apps/web/app/\(portfolio\) \
  /home/doctor/fernandomillan/apps/web/components/portfolio

# Confirm ProjectCard import in projects/page.tsx still uses migrated component
grep "project-card" /home/doctor/fernandomillan/apps/web/app/\(portfolio\)/projects/page.tsx

# TypeScript compiles cleanly
cd /home/doctor/fernandomillan && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | grep -E "error|Error" | head -20
```
  </verify>
  <done>
The comprehensive grep across `app/(portfolio)` and `components/portfolio` returns zero hardcoded color patterns. `projects/page.tsx` still imports `ProjectCard` from its expected path. TypeScript compilation passes with zero errors.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "bg-white|bg-gray-|text-gray-|border-gray-|bg-blue-|dark:bg-gray|dark:text-gray" apps/web/app/(portfolio) apps/web/components/portfolio` returns zero results
2. `apps/web/app/(portfolio)/page.tsx` imports `Badge` from `@/components/ui/badge`
3. `apps/web/app/(portfolio)/about/page.tsx` imports `Card` from `@/components/ui/card`
4. `apps/web/app/(portfolio)/resume/page.tsx` imports `Button` from `@/components/ui/button`
5. `apps/web/app/(portfolio)/projects/teamflow/page.tsx` uses `Button asChild` for CTA links
6. TypeScript compilation passes
</verification>

<success_criteria>
All five portfolio pages migrated. Zero hardcoded Tailwind color classes remain in the entire portfolio scope (app/(portfolio) + components/portfolio). TypeScript compiles cleanly. MIG-01 requirement fully satisfied: grep search for old component imports and hardcoded classes returns zero results.
</success_criteria>

<output>
After completion, create `.planning/phases/10-component-migration-portfolio/10-03-SUMMARY.md`
</output>
