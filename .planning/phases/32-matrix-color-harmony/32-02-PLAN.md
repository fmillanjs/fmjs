---
phase: 32-matrix-color-harmony
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - apps/web/app/(portfolio)/contact/page.tsx
  - apps/web/app/(portfolio)/projects/teamflow/page.tsx
  - apps/web/app/(portfolio)/projects/devcollab/page.tsx
  - apps/web/components/portfolio/case-study-section.tsx
  - apps/web/app/(portfolio)/resume/page.tsx
autonomous: true
requirements:
  - COLOR-03
  - COLOR-04
  - COLOR-07

must_haves:
  truths:
    - "Contact page email link and any text-primary references use Matrix green — no blue primary color visible"
    - "Contact page h2 headings have SectionLabel prefix above them"
    - "TeamFlow and DevCollab case study metric numbers are font-mono Matrix green (--matrix-terminal)"
    - "TeamFlow and DevCollab border-l-4 border-primary challenge sections use Matrix green border token"
    - "Case study Back to Projects links use Matrix green instead of blue"
    - "CaseStudySection component adds SectionLabel prefix above its h2 — all case study section headings automatically covered"
    - "Resume page h2 headings each have SectionLabel prefix above them"
    - "Resume page view case study link uses Matrix green"
  artifacts:
    - path: "apps/web/app/(portfolio)/contact/page.tsx"
      provides: "Contact page with Matrix green — zero blue primary"
    - path: "apps/web/app/(portfolio)/projects/teamflow/page.tsx"
      provides: "TeamFlow page — metric numbers monospace Matrix green, challenge borders green"
    - path: "apps/web/app/(portfolio)/projects/devcollab/page.tsx"
      provides: "DevCollab page — metric numbers monospace Matrix green, challenge borders green"
    - path: "apps/web/components/portfolio/case-study-section.tsx"
      provides: "CaseStudySection adds SectionLabel above each h2"
      contains: "SectionLabel"
    - path: "apps/web/app/(portfolio)/resume/page.tsx"
      provides: "Resume page — h2 headings with SectionLabel prefix, links Matrix green"
  key_links:
    - from: "apps/web/components/portfolio/case-study-section.tsx"
      to: "apps/web/components/portfolio/section-label.tsx"
      via: "import { SectionLabel } from './section-label'"
      pattern: "SectionLabel"
    - from: "apps/web/app/(portfolio)/resume/page.tsx"
      to: "apps/web/components/portfolio/section-label.tsx"
      via: "import { SectionLabel } from '@/components/portfolio/section-label'"
      pattern: "SectionLabel"
---

<objective>
Apply Matrix color harmony to the contact page (COLOR-03), both case study pages (COLOR-04), the CaseStudySection shared component, and the resume page. Add SectionLabel terminal prefixes to all h2 headings across these pages (COLOR-07 partial).

Purpose: Parallel to 32-03 — no shared files. This plan clears all blue primary references from contact, case study, and resume routes. The CaseStudySection component update propagates SectionLabel to all 12+ case study h2 headings with a single change.

Output: contact, teamflow, devcollab, resume pages and CaseStudySection component fully Matrix green.
</objective>

<execution_context>
@/home/doctor/.claude/get-shit-done/workflows/execute-plan.md
@/home/doctor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/32-matrix-color-harmony/32-RESEARCH.md
@.planning/phases/32-matrix-color-harmony/32-01-SUMMARY.md

@apps/web/app/(portfolio)/contact/page.tsx
@apps/web/components/portfolio/case-study-section.tsx
@apps/web/app/(portfolio)/projects/teamflow/page.tsx
@apps/web/app/(portfolio)/projects/devcollab/page.tsx
@apps/web/app/(portfolio)/resume/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contact page (COLOR-03) + CaseStudySection SectionLabel + resume page (COLOR-07)</name>
  <files>
    apps/web/app/(portfolio)/contact/page.tsx
    apps/web/components/portfolio/case-study-section.tsx
    apps/web/app/(portfolio)/resume/page.tsx
  </files>
  <action>
**contact/page.tsx — COLOR-03:**

Read the file first. Then:

1. Add import: `import { SectionLabel } from '@/components/portfolio/section-label';`

2. Find `text-primary hover:underline` on the email link (research confirmed line 34). Replace:
   `text-primary hover:underline` → `text-[var(--matrix-green)] hover:underline`

3. Add `<SectionLabel>` above each `<h2>`:
   - Above `<h2 ... >Send a Message</h2>` → `<SectionLabel label="Send a Message" />`
   - Above `<h2 ... >Contact Information</h2>` → `<SectionLabel label="Contact Information" />`

**case-study-section.tsx — COLOR-07 propagation:**

Read the file. Add SectionLabel import and insert it above the existing `<h2>`:

```tsx
import { SectionLabel } from './section-label';

// Inside the component JSX, before the existing <h2>:
<SectionLabel label={title} />
<h2 className="mb-4 text-2xl font-bold">{title}</h2>
```

This single change propagates the terminal label prefix to ALL case study section headings across both TeamFlow and DevCollab pages automatically via the shared CaseStudySection component.

**resume/page.tsx — COLOR-07:**

1. Add import: `import { SectionLabel } from '@/components/portfolio/section-label';`

2. Research confirmed 5 h2 headings at lines ~36, 50, 106, 152, 176. Add `<SectionLabel>` above each. Use the text content of the h2 as the label. Examples:
   - `<SectionLabel label="Professional Summary" />` above the Summary h2
   - `<SectionLabel label="Technical Skills" />` above Technical Skills h2
   - `<SectionLabel label="Experience" />` above Experience h2
   - `<SectionLabel label="Education" />` above Education h2
   - `<SectionLabel label="Projects" />` above Projects h2

   The existing `border-b border-border pb-2` on the resume h2s stays — SectionLabel goes ABOVE the h2, not replacing its border.

3. Find `text-primary hover:underline` on the "View Case Study →" link (research confirmed line 191). Replace with `text-[var(--matrix-green)] hover:underline`.
  </action>
  <verify>
grep -n "primary" apps/web/app/(portfolio)/contact/page.tsx
grep -n "SectionLabel" apps/web/components/portfolio/case-study-section.tsx
grep -n "primary" apps/web/app/(portfolio)/resume/page.tsx
grep -n "SectionLabel" apps/web/app/(portfolio)/resume/page.tsx | wc -l
cd apps/web && npx tsc --noEmit 2>&1 | head -20
  </verify>
  <done>contact/page.tsx has zero primary references and SectionLabel above each h2. case-study-section.tsx imports SectionLabel and renders it above the h2. resume/page.tsx has SectionLabel above all 5 h2 headings and zero primary references. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: TeamFlow and DevCollab case study pages (COLOR-04)</name>
  <files>
    apps/web/app/(portfolio)/projects/teamflow/page.tsx
    apps/web/app/(portfolio)/projects/devcollab/page.tsx
  </files>
  <action>
**teamflow/page.tsx — COLOR-04:**

Read the file first. Make these changes (research verified line numbers, but search by pattern since line numbers may drift after 32-01):

1. **Metric numbers** (research: lines 87, 95, 103 — `text-3xl font-bold text-primary`):
   Replace `text-primary` with `font-mono text-[var(--matrix-terminal)]` on each metric stat div. Result: `text-3xl font-bold font-mono text-[var(--matrix-terminal)] mb-2`

2. **Inline text-primary links** (research: lines 82, 125, 502, 503, 504):
   Replace `text-primary` with `text-[var(--matrix-green)]` on these inline text/link elements.

3. **Back to Projects link** (research: line 27):
   Replace `text-primary` with `text-[var(--matrix-green)]` on the "Back to Projects" link.

4. **Challenge section borders** (research: lines 410, 434, 456 — `border-l-4 border-primary`):
   Replace `border-primary` with `border-[var(--matrix-green-border)]` on all 3 challenge section divs.

After changes, verify: `grep -n "text-primary\|border-primary\|from-primary\|bg-primary" apps/web/app/(portfolio)/projects/teamflow/page.tsx` should return 0 results.

**devcollab/page.tsx — COLOR-04:**

Read the file first. Make these changes:

1. **Metric numbers** (research: lines 90, 94, 98 — `text-3xl font-bold text-primary`):
   Replace `text-primary` with `font-mono text-[var(--matrix-terminal)]` on each metric stat div.

2. **Back to Projects link** (research: line 29):
   Replace `text-primary` with `text-[var(--matrix-green)]`.

3. **Challenge section borders** (research: lines 282, 305, 327 — `border-l-4 border-primary`):
   Replace `border-primary` with `border-[var(--matrix-green-border)]` on all 3.

After changes, verify: `grep -n "text-primary\|border-primary\|from-primary\|bg-primary" apps/web/app/(portfolio)/projects/devcollab/page.tsx` should return 0 results.

Note: SectionLabel is NOT needed inline in these files — CaseStudySection component (patched in Task 1) automatically renders the prefix for all case study section h2 headings. Only inline h2 outside of CaseStudySection (e.g., the page-level hero h1) would need manual treatment, but case study pages use h1 for their main title.
  </action>
  <verify>
grep -n "text-primary\|border-primary\|from-primary\|bg-primary" apps/web/app/(portfolio)/projects/teamflow/page.tsx
grep -n "text-primary\|border-primary\|from-primary\|bg-primary" apps/web/app/(portfolio)/projects/devcollab/page.tsx
grep -n "font-mono.*matrix-terminal\|matrix-terminal.*font-mono" apps/web/app/(portfolio)/projects/teamflow/page.tsx | wc -l
cd apps/web && npx tsc --noEmit 2>&1 | head -20
  </verify>
  <done>Both teamflow/page.tsx and devcollab/page.tsx have zero primary color references. Metric stat numbers use font-mono + text-[var(--matrix-terminal)]. Challenge section borders use border-[var(--matrix-green-border)]. TypeScript compilation clean.</done>
</task>

</tasks>

<verification>
1. `grep -rn "text-primary\|border-primary\|from-primary\|bg-primary" apps/web/app/(portfolio)/contact/ apps/web/app/(portfolio)/projects/ apps/web/app/(portfolio)/resume/` — zero results
2. `grep -n "SectionLabel" apps/web/components/portfolio/case-study-section.tsx` — renders SectionLabel before h2
3. `grep -n "SectionLabel" apps/web/app/(portfolio)/resume/page.tsx | wc -l` — 6 lines (1 import + 5 usage)
4. `cd apps/web && npx tsc --noEmit` — clean
</verification>

<success_criteria>
- Contact page: text-primary on email link replaced with Matrix green; SectionLabel above both h2 headings
- CaseStudySection: SectionLabel imported and rendered above h2 — propagates to TeamFlow and DevCollab automatically
- TeamFlow page: metric numbers font-mono Matrix green, challenge borders green, all primary refs gone
- DevCollab page: metric numbers font-mono Matrix green, challenge borders green, all primary refs gone
- Resume page: SectionLabel above all 5 h2 headings, view case study link Matrix green
- TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/32-matrix-color-harmony/32-02-SUMMARY.md` following the summary template.
</output>
