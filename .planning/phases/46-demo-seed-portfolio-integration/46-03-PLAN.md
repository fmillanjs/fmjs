---
phase: 46-demo-seed-portfolio-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 46-02
files_modified:
  - apps/web/app/(portfolio)/page.tsx
  - apps/web/app/(portfolio)/projects/page.tsx
  - apps/web/app/(portfolio)/projects/ai-sdr/page.tsx
  - apps/web/src/data/walkthrough-data.ts
autonomous: false
requirements:
  - PORT-01
  - PORT-02
  - PORT-03

must_haves:
  truths:
    - Portfolio home page (/) shows 3 project cards including AI SDR (PORT-01)
    - /projects/ai-sdr renders a complete case study with tech stack badges, architecture decisions table, and challenges section (PORT-02)
    - "View Live Demo" button on /projects/ai-sdr links to the deployed AI SDR URL (PORT-03)
    - No purple colors are used anywhere in the new pages (global constraint from REQUIREMENTS.md and user settings)
    - AI SDR case study uses the same CaseStudySection + WalkthroughSection component pattern as teamflow/page.tsx
    - walkthrough-data.ts exports AI_SDR_WALKTHROUGH_SCREENSHOTS constant
    - WalkthroughSection on /projects/ai-sdr references 4 screenshot files at /screenshots/ai-sdr-*.png
  artifacts:
    - apps/web/app/(portfolio)/projects/ai-sdr/page.tsx
    - apps/web/src/data/walkthrough-data.ts (updated)
  key_links:
    - Portfolio home grid changes from md:grid-cols-2 to lg:grid-cols-3 (3rd card)
    - Case study must import AI_SDR_WALKTHROUGH_SCREENSHOTS from walkthrough-data.ts
    - Screenshot files at /screenshots/ai-sdr-leads.png, ai-sdr-pipeline.png, ai-sdr-score.png, ai-sdr-email.png (taken by human)
    - Live demo URL from Plan 46-02 checkpoint (https://ai-sdr.fernandomillan.me)
---

<objective>
Add AI SDR as a third project to the portfolio — including a project card on the home page, an entry in the /projects listing, and a full case study page at /projects/ai-sdr with tech stack, architecture decisions, and app walkthrough screenshots.

Purpose: Recruiters visiting fernandomillan.me see three production-ready projects (PORT-01). Clicking AI SDR opens a case study that demonstrates AI, streaming, and backend engineering depth (PORT-02). The "View Live Demo" button lets them interact with the deployed app (PORT-03).

Output: `apps/web/app/(portfolio)/projects/ai-sdr/page.tsx`, updated `walkthrough-data.ts`, updated home page and projects listing.
</objective>

<execution_context>
@/home/doctor/.claude/get-shit-done/workflows/execute-plan.md
@/home/doctor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-demo-seed-portfolio-integration/46-RESEARCH.md

<interfaces>
<!-- Verified: apps/web/app/(portfolio)/page.tsx — current 2-column grid to update -->
<StaggerContainer className="grid grid-cols-1 md:grid-cols-2 gap-6 max-w-6xl mx-auto">
  <!-- TeamFlow card -->
  <!-- DevCollab card -->
</StaggerContainer>
// Change to: grid-cols-1 md:grid-cols-2 lg:grid-cols-3
// Update p tag: "Two production-ready..." → "Three production-ready..."

<!-- Verified: apps/web/app/(portfolio)/projects/page.tsx — 2-column grid to add AI SDR -->
<StaggerContainer className="grid gap-6 md:grid-cols-2">
  <StaggerItem><ProjectCard title="TeamFlow" .../></StaggerItem>
  <StaggerItem><ProjectCard title="DevCollab" .../></StaggerItem>
</StaggerContainer>

<!-- Verified: apps/web/app/(portfolio)/projects/teamflow/page.tsx — exact pattern to follow -->
import { CaseStudySection } from '@/components/portfolio/case-study-section';
import { WalkthroughSection } from '@/components/portfolio/walkthrough-section';
import { TEAMFLOW_WALKTHROUGH_SCREENSHOTS } from '@/src/data/walkthrough-data';
// Use: CaseStudySection, AnimateIn, Button (with ExternalLink + Github icons), WalkthroughSection

<!-- Verified: apps/web/src/data/walkthrough-data.ts — export pattern -->
import type { WalkthroughScreenshot } from '@/components/portfolio/walkthrough-section'
export const TEAMFLOW_WALKTHROUGH_SCREENSHOTS: WalkthroughScreenshot[] = [
  {
    src: '/screenshots/teamflow-kanban.png',
    width: 1280,
    height: 800,
    alt: '...',
    steps: [
      { label: '...', explanation: '...' },
    ],
  },
]
// Add AI_SDR_WALKTHROUGH_SCREENSHOTS constant with same structure

<!-- Design constraints (from REQUIREMENTS.md + user global settings) -->
- NO purple colors anywhere — use existing Radix slate/blue/green/amber color system
- Use var(--matrix-green), var(--matrix-green-border), var(--matrix-green-subtle) for accent colors
- Use var(--matrix-terminal) for mono stat numbers
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Take 4 screenshots from the running AI SDR app</name>
  <files>apps/web/public/screenshots/ai-sdr-leads.png, apps/web/public/screenshots/ai-sdr-pipeline.png, apps/web/public/screenshots/ai-sdr-score.png, apps/web/public/screenshots/ai-sdr-email.png</files>
  <action>
The human must take 4 screenshots from the deployed AI SDR app (https://ai-sdr.fernandomillan.me) or the local running instance. Screenshots must be exactly **1280x800 pixels**.

**Screenshot 1: ai-sdr-leads.png**
- URL: /leads
- What to capture: The lead table showing all 8 seeded leads with ICP score bars, status badges, and company names visible
- The score bars should be visible and color-coded (green for high scores, amber for medium, red for low)

**Screenshot 2: ai-sdr-pipeline.png**
- URL: /leads/{any-pending-lead-id} (submit a new lead and capture mid-pipeline)
- What to capture: The PipelineMonitor during active processing — showing step progress indicators (qualify/enrich/personalize steps, with at least one checked and one in-progress)

**Screenshot 3: ai-sdr-score.png**
- URL: /leads/{any-complete-lead-id}
- What to capture: The ScoreCard showing the ICP score horizontal bar (colored, with numeric label) and the WhyScoreCard expanded to show matched and weak criteria bullets

**Screenshot 4: ai-sdr-email.png**
- URL: /leads/{same-complete-lead-id}
- What to capture: The EmailPreview card showing the complete generated email text, with the "Copy Email" button visible

**How to take 1280x800 screenshots:**
- Chrome DevTools → Device toolbar → set custom 1280x800
- Or use: `npx playwright screenshot --viewport-size=1280,800`
- Save to: `apps/web/public/screenshots/` with the exact filenames above

After saving all 4 screenshots, confirm the files exist:
```bash
ls -la /home/doctor/fernandomillan/apps/web/public/screenshots/ai-sdr-*.png
```
  </action>
  <verify>
    <automated>ls /home/doctor/fernandomillan/apps/web/public/screenshots/ai-sdr-leads.png /home/doctor/fernandomillan/apps/web/public/screenshots/ai-sdr-pipeline.png /home/doctor/fernandomillan/apps/web/public/screenshots/ai-sdr-score.png /home/doctor/fernandomillan/apps/web/public/screenshots/ai-sdr-email.png 2>&1</automated>
  </verify>
  <done>
    All 4 PNG files exist at apps/web/public/screenshots/ai-sdr-*.png at 1280x800 resolution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add AI_SDR_WALKTHROUGH_SCREENSHOTS to walkthrough-data.ts and create case study page</name>
  <files>apps/web/src/data/walkthrough-data.ts, apps/web/app/(portfolio)/projects/ai-sdr/page.tsx</files>
  <action>
**Step 1: Update `apps/web/src/data/walkthrough-data.ts`**

Append the following export after the existing DEVCOLLAB_WALKTHROUGH_SCREENSHOTS constant (do NOT modify existing exports):

```typescript
export const AI_SDR_WALKTHROUGH_SCREENSHOTS: WalkthroughScreenshot[] = [
  {
    src: '/screenshots/ai-sdr-leads.png',
    width: 1280,
    height: 800,
    alt: 'AI SDR CRM dashboard showing 8 pre-seeded leads in a table with color-coded ICP score bars ranging from red (low fit) to green (high fit)',
    steps: [
      {
        label: 'ICP Score Bars',
        explanation: 'Color-coded bars (green 70+, amber 40-69, red below 40) show qualification fit at a glance — no column sorting needed to identify top prospects.',
      },
      {
        label: 'Pre-Seeded Demo Data',
        explanation: 'Eight leads spanning FinTech, Healthcare, DevTools, and Logistics are loaded on first login — no setup required for recruiters to explore the full feature set.',
      },
      {
        label: 'Status Column',
        explanation: 'The status badge (pending / processing / complete / failed) reflects the pipeline state so reps know which leads have AI outputs ready to review.',
      },
    ],
  },
  {
    src: '/screenshots/ai-sdr-pipeline.png',
    width: 1280,
    height: 800,
    alt: 'AI SDR lead detail page showing real-time pipeline progress with step indicators for qualify, enrich, and personalize steps, one step completed and one in progress',
    steps: [
      {
        label: 'Step Progress Indicators',
        explanation: 'Each pipeline step (qualify, enrich, personalize) shows a spinner while Claude API runs, then a checkmark when complete — giving the user live feedback without polling.',
      },
      {
        label: 'SSE Streaming',
        explanation: 'Progress arrives via Server-Sent Events — the NestJS backend streams step completion events as the pipeline runs, not in a single batch at the end.',
      },
      {
        label: 'Non-Blocking Submission',
        explanation: 'The lead is created immediately with status pending, then the pipeline triggers on first SSE connection — the form submission returns instantly with no artificial wait.',
      },
    ],
  },
  {
    src: '/screenshots/ai-sdr-score.png',
    width: 1280,
    height: 800,
    alt: 'AI SDR lead detail page showing the ICP ScoreCard with a colored horizontal score bar at 85 and the Why This Score accordion expanded with matched and weak criteria bullets',
    steps: [
      {
        label: 'ICP Score Bar',
        explanation: 'The horizontal bar renders in green (70+), amber (40-69), or red (below 40) and shows the numeric score — communicating qualification strength without any jargon.',
      },
      {
        label: 'Why This Score Accordion',
        explanation: 'The collapsible card lists matched ICP criteria (green checkmarks) and weak criteria (amber warnings) — so reps understand why Claude scored the lead, not just what the score is.',
      },
      {
        label: 'Structured Output',
        explanation: 'Score and reasoning come from Claude structured outputs via Zod schemas at temperature 0 — the same input always produces the same score, making the ICP rubric auditable and deterministic.',
      },
    ],
  },
  {
    src: '/screenshots/ai-sdr-email.png',
    width: 1280,
    height: 800,
    alt: 'AI SDR lead detail page showing the EmailPreview card with a fully generated personalized cold email and a Copy Email button',
    steps: [
      {
        label: 'Streaming Email Preview',
        explanation: 'The email arrives token-by-token from Claude via SSE — the user watches it being written in real time rather than waiting for a completed response.',
      },
      {
        label: 'Company-Specific Copy',
        explanation: 'The email references company-specific facts from the enrichment step (funding stage, tech stack, pain points) — not generic templates that buyers ignore.',
      },
      {
        label: 'One-Click Copy',
        explanation: 'The Copy Email button uses navigator.clipboard.writeText() to transfer the full email to clipboard instantly — ready to paste into any outreach tool.',
      },
    ],
  },
]
```

**Step 2: Create `apps/web/app/(portfolio)/projects/ai-sdr/page.tsx`**

Follow the exact same pattern as `apps/web/app/(portfolio)/projects/teamflow/page.tsx`. Use CaseStudySection, AnimateIn, Button (with ExternalLink and Github icons), WalkthroughSection. The live demo URL is `https://ai-sdr.fernandomillan.me/login` (update if the actual deployed URL from Plan 46-02 is different).

Structure:
1. Header with back link, H1 "AI SDR", subtitle, and two buttons (View Live Demo + View Source)
2. Overview section — what it is, 3 stat blocks (Pipeline Steps, ICP Scoring, Live Streaming), features list
3. Problem section — what problem it solves for SDR teams
4. Solution section — how the AI pipeline addresses each problem
5. Architecture section — browser → Next.js → NestJS → Claude API → Postgres flow diagram, standalone repo structure, frontend/backend/data layer breakdown
6. Key Technical Decisions section — decisions table (same format as teamflow)
7. Challenges & Solutions section — 3 challenges with solutions
8. App Walkthrough section (WalkthroughSection component)
9. Results section with Try the Demo callout box

**Metadata:**
```typescript
export const metadata: Metadata = {
  title: 'AI SDR - Case Study',
  description: 'A production-ready AI Sales Development Representative system using the Claude API for lead qualification, CRM enrichment, and personalized cold email generation with real-time SSE streaming.',
  openGraph: {
    title: 'AI SDR Case Study - Fernando Millan',
    description: 'AI-powered lead qualification, CRM enrichment, and email personalization with Claude structured outputs and SSE token streaming.',
    type: 'article',
  },
};
```

**Tech stack badges for the overview:**
`['Next.js 16', 'NestJS 11', 'TypeScript', 'Claude API', 'Anthropic SDK', 'Prisma', 'PostgreSQL', 'Docker', 'Tailwind', 'SSE']`

**Architecture diagram (mono block):**
```
Browser (Next.js Client Components)
  ↓ EventSource (SSE)
Next.js App Router (RSC + Server Actions)
  ↓ HTTP REST + SSE
NestJS Backend (LeadsController → PipelineService)
  ↓ Anthropic SDK
Claude claude-sonnet-4-6 (qualify → enrich → personalize)
  ↓ Prisma ORM
PostgreSQL (Lead, AIOutput, DemoLead tables)
```

**Key Technical Decisions table (6 rows):**
1. SSE over WebSockets — unidirectional pipeline events (qualify→enrich→personalize) need no bidirectional channel; SSE is simpler, works through Nginx with X-Accel-Buffering: no, and has no reconnect complexity
2. Structured outputs (Zod + zodOutputFormat) — qualify and enrich steps return typed JSON via Anthropic's native structured output; temperature: 0 ensures deterministic ICP scores across repeated calls for the same input
3. Standalone repo (not Turborepo monorepo) — ai-sdr is fully self-contained to prove ability to architect either way; no shared packages, simpler Docker build context (./ai-sdr), no turbo prune needed
4. Pre-seeded demo data — live scraping fails 20-30% of sites and takes 45-90 seconds per lead; seed script writes hand-authored AI outputs directly to Postgres so recruiters see a populated app immediately with zero latency
5. In-process pipeline (no Redis/BullMQ) — sequential steps (qualify → enrich → personalize) run synchronously in PipelineService; callback bridge pattern connects StepCallback to SSE Observable without any queue infrastructure
6. Separate NEXT_PUBLIC_API_URL at build time — Next.js bakes NEXT_PUBLIC_ variables into the client bundle during next build; Docker ARG passes the API URL at image build time, not runtime, to correctly wire the EventSource URL in Client Components

**Challenges & Solutions (3 entries):**

Challenge 1: SSE Streaming Through Nginx Reverse Proxy
- Problem: Nginx buffers SSE responses by default, delivering all tokens in a single batch at the end instead of incrementally. This breaks the real-time streaming experience.
- Solution: Set `X-Accel-Buffering: no` as a per-request response header in NestJS (leads.controller.ts). Nginx 1.1.4+ honors this header per-connection without requiring server config changes — zero-config Coolify deployment.
- Learned: Nginx buffering is silent — SSE appears to work locally (no proxy) but fails in production. The X-Accel-Buffering header is the correct solution; Nginx configuration changes would require Coolify infrastructure access.

Challenge 2: Observable/Callback Bridge for NestJS SSE
- Problem: NestJS @Sse() expects an Observable<MessageEvent>, but the pipeline runs via async callbacks (StepCallback). Bridging the two without leaking Claude API connections on client disconnect.
- Solution: Wrapped the pipeline in `new Observable<MessageEvent>(subscriber => ...)` and used a `closed` boolean flag + subscriber.closed guard in the onStep callback to prevent "Cannot call next on closed subscriber" errors. Added `res.on('close', () => closed = true)` for explicit disconnect detection.
- Learned: The Observable/callback bridge pattern is the cleanest way to connect imperative async code to reactive streams in NestJS. The closed flag is essential — without it, a client disconnect during the email streaming step causes an unhandled error.

Challenge 3: Zod v4 + Anthropic Structured Outputs
- Problem: The @anthropic-ai/sdk's zodOutputFormat() calls z.toJSONSchema() which only exists in Zod v4+. Zod v3 (default install) causes a silent failure where structured output schema is undefined.
- Solution: Upgraded to zod@4.3.6. Also confirmed that nullable() (not optional()) must be used for optional EnrichOutput fields — Anthropic structured outputs require all fields in the JSON schema's required array; optional() would cause the API to reject the schema.
- Learned: Structured output schema compatibility is fragile when mixing Zod versions. Pinning to zod@^4.x in package.json prevents silent schema failures.

**Results section stat grid:**
- Pipeline Steps: 3 (qualify → enrich → personalize)
- Response Time: Real-Time (SSE streaming)
- Deployment: Production

**Try the Demo callout:**
- Demo credentials shown on login page
- URL: https://ai-sdr.fernandomillan.me/login
- Instruction: "Log in to see 8 pre-seeded leads with ICP scores across industries. Submit a new lead to watch Claude qualify, enrich, and write a personalized email in real time."

**CRITICAL design rules:**
- Do NOT use purple anywhere (background, text, badges, borders)
- Use `var(--matrix-green)` for accent text, `var(--matrix-green-border)` for borders, `var(--matrix-terminal)` for mono stat numbers
- Use `border-[var(--matrix-green-border)]` for the "Try the Demo" callout border
- Copy exact className patterns from teamflow/page.tsx for all CaseStudySection content
  </action>
  <verify>
    <automated>ls /home/doctor/fernandomillan/apps/web/app/(portfolio)/projects/ai-sdr/page.tsx && grep "AI_SDR_WALKTHROUGH_SCREENSHOTS" /home/doctor/fernandomillan/apps/web/src/data/walkthrough-data.ts</automated>
  </verify>
  <done>
    `/projects/ai-sdr/page.tsx` exists and imports `AI_SDR_WALKTHROUGH_SCREENSHOTS` from walkthrough-data.ts. `walkthrough-data.ts` exports `AI_SDR_WALKTHROUGH_SCREENSHOTS` with 4 screenshot entries. The case study page has all required sections (Overview, Problem, Solution, Architecture, Key Technical Decisions, Challenges, Walkthrough, Results).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update portfolio home page and projects listing to add AI SDR card</name>
  <files>apps/web/app/(portfolio)/page.tsx, apps/web/app/(portfolio)/projects/page.tsx</files>
  <action>
**Update `apps/web/app/(portfolio)/page.tsx`:**

1. Change the StaggerContainer grid className from `"grid grid-cols-1 md:grid-cols-2 gap-6 max-w-6xl mx-auto"` to `"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto"`

2. Update the paragraph text from `"Two production-ready SaaS applications showcasing full-stack expertise"` to `"Three production-ready SaaS applications showcasing full-stack and AI engineering expertise"`

3. Add the AI SDR StaggerItem card after the DevCollab StaggerItem, using the same Link + EvervaultCard pattern:

```tsx
{/* AI SDR Card */}
<StaggerItem>
<Link
  href="/projects/ai-sdr"
  className="card-glow-hover block border-2 border-[var(--matrix-green-border)] rounded-lg p-8 bg-gradient-to-br from-[var(--matrix-green-subtle)] to-transparent"
>
  <EvervaultCard className="min-h-full">
    <div className="mb-6">
      <h3 className="text-2xl md:text-3xl font-bold text-foreground mb-2">
        AI SDR
      </h3>
      <p className="text-base text-muted-foreground">
        AI Sales Development Representative System
      </p>
    </div>

    <p className="text-foreground mb-6 leading-relaxed">
      A standalone AI SDR replacement system using the Claude API for lead qualification,
      CRM enrichment, and personalized cold email generation. Features real-time SSE token
      streaming, Zod structured outputs at temperature 0, and a pre-seeded demo with
      ICP scores across 8 industries.
    </p>

    {/* Tech Stack Badges */}
    <div className="flex flex-wrap gap-2 mb-6">
      {[
        'Next.js 16',
        'NestJS',
        'TypeScript',
        'Claude API',
        'Prisma',
        'PostgreSQL',
        'SSE',
        'Docker',
        'Tailwind',
      ].map((tech) => (
        <Badge key={tech} variant="secondary">
          {tech}
        </Badge>
      ))}
    </div>

    <div className="text-[var(--matrix-green)] font-medium hover:underline">
      Read full case study →
    </div>
  </EvervaultCard>
</Link>
</StaggerItem>
```

**Update `apps/web/app/(portfolio)/projects/page.tsx`:**

1. Change the StaggerContainer grid from `"grid gap-6 md:grid-cols-2"` to `"grid gap-6 md:grid-cols-2 lg:grid-cols-3"`

2. Add AI SDR ProjectCard after the DevCollab StaggerItem:

```tsx
<StaggerItem>
  <ProjectCard
    title="AI SDR"
    description="An AI Sales Development Representative system using Claude for lead qualification, CRM enrichment, and personalized email generation with real-time SSE streaming"
    techStack={['Next.js 16', 'NestJS', 'TypeScript', 'Claude API', 'Prisma', 'PostgreSQL', 'SSE', 'Docker']}
    href="/projects/ai-sdr"
    featured
    screenshot={{ src: '/screenshots/ai-sdr-leads.png', alt: 'AI SDR CRM dashboard with lead scoring' }}
  />
</StaggerItem>
```

**CRITICAL:** Do NOT use purple anywhere. All badges use `variant="secondary"` (existing Tailwind style). The card background uses `from-[var(--matrix-green-subtle)]` (green-tinted, not blue or purple).
  </action>
  <verify>
    <automated>grep -c "AI SDR" /home/doctor/fernandomillan/apps/web/app/(portfolio)/page.tsx && grep -c "ai-sdr" /home/doctor/fernandomillan/apps/web/app/(portfolio)/projects/page.tsx</automated>
  </verify>
  <done>
    `apps/web/app/(portfolio)/page.tsx` has the AI SDR card and `lg:grid-cols-3` and updated subtitle. `apps/web/app/(portfolio)/projects/page.tsx` has the AI SDR ProjectCard. No purple colors in either file.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Human verification — portfolio pages render correctly</name>
  <files></files>
  <action>
Run the portfolio locally and verify all three pages:

```bash
cd /home/doctor/fernandomillan && npm run dev
# or: cd apps/web && npm run dev
```

Then open browser at http://localhost:3000:

1. **Home page (/):**
   - Confirm 3 project cards are visible: TeamFlow, DevCollab, AI SDR
   - On desktop (>= 1024px): all 3 cards are in a single row (lg:grid-cols-3)
   - On tablet (768-1023px): TeamFlow + DevCollab on row 1, AI SDR below (md:grid-cols-2 wraps)
   - AI SDR card shows tech stack badges, description, and "Read full case study →" link
   - No purple anywhere in the card or surrounding layout

2. **Projects page (/projects):**
   - AI SDR ProjectCard appears after DevCollab
   - Screenshot thumbnail (ai-sdr-leads.png) renders without broken image
   - Tech stack badges are visible

3. **AI SDR case study (/projects/ai-sdr):**
   - "Back to Projects" link renders correctly
   - H1 "AI SDR" and subtitle are visible
   - "View Live Demo" button links to https://ai-sdr.fernandomillan.me/login (opens in new tab)
   - "View Source" button links to the GitHub repo
   - Overview section with 3 stat blocks renders
   - Features list is visible
   - Architecture section with code block renders
   - Key Technical Decisions table renders (6 rows, no purple)
   - Challenges & Solutions section renders with 3 challenge blocks
   - App Walkthrough section shows 4 screenshots with step annotations
   - Results section with "Try the Demo" callout box renders

4. **Live demo link (PORT-03):**
   - Click "View Live Demo" → confirms it navigates to https://ai-sdr.fernandomillan.me/login
  </action>
  <verify>
    <automated>MISSING — visual verification requires browser</automated>
  </verify>
  <done>
    Human confirms: (1) 3 project cards on home page with no purple, (2) /projects shows AI SDR card with screenshot, (3) /projects/ai-sdr case study fully renders with all sections, (4) "View Live Demo" button navigates to the deployed AI SDR app.
  </done>
</task>

</tasks>

<verification>
After all tasks (including human checkpoint):

1. Build check — portfolio builds without TypeScript errors:
   ```bash
   cd /home/doctor/fernandomillan/apps/web && npm run build
   # Expected: Build successful, no TypeScript errors
   ```

2. Check for purple (must return 0 matches):
   ```bash
   grep -r "purple\|#[0-9a-f]*b[0-9a-f]*\|rgb(.*128.*0.*128" /home/doctor/fernandomillan/apps/web/app/(portfolio)/projects/ai-sdr/ 2>/dev/null | grep -v ".git" | wc -l
   # Expected: 0
   ```

3. Screenshot files exist:
   ```bash
   ls /home/doctor/fernandomillan/apps/web/public/screenshots/ai-sdr-*.png
   # Expected: 4 files (leads, pipeline, score, email)
   ```

4. Walkthrough data export exists:
   ```bash
   grep "AI_SDR_WALKTHROUGH_SCREENSHOTS" /home/doctor/fernandomillan/apps/web/src/data/walkthrough-data.ts
   # Expected: export const AI_SDR_WALKTHROUGH_SCREENSHOTS
   ```
</verification>

<success_criteria>
- PORT-01: Portfolio home page at fernandomillan.me displays 3 project cards including AI SDR
- PORT-02: /projects/ai-sdr case study renders with tech stack, architecture diagram, decisions table, and 4 app screenshots
- PORT-03: "View Live Demo" button links to the deployed AI SDR app (verified clickable in production)
- No purple colors anywhere (global constraint from REQUIREMENTS.md and user settings)
- Phase 46 complete — all 5 requirements satisfied (DEMO-01, DEMO-02, PORT-01, PORT-02, PORT-03)
</success_criteria>

<output>
After completion, create `.planning/phases/46-demo-seed-portfolio-integration/46-03-SUMMARY.md`
</output>
